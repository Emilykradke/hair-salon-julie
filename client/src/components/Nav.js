import React, { Component } from 'react';
import Scrollspy from 'react-scrollspy'

class Nav extends Component {
  render() {
    return (
      <nav>
        <Scrollspy>
          
        </Scrollspy>
      </nav>
    );
  }
}

export default Nav;